using Friflo.Engine.ECS;
namespace Presentation.HexResources.Tags
{
    // Discriminator: present on every ForestView entity; filters must combine it with the key component.
    public struct ForestViewTag : ITag
    {
    }
}
