namespace Presentation.HexIcons.Tags
{
    // Discriminator: present on every HexIconContainer entity; filters must combine it with the key component.
    public struct HexIconContainerTag
    {
    }
}
