namespace Presentation.UI.DistrictBuild.Data
{
    // Which actor pays the resource cost (the district owner). AP is always Mayor-paid, regardless of this choice.
    public enum Payer
    {
        Mayor,
        City
    }
}
