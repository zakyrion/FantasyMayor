using Friflo.Engine.ECS;

namespace Domains.Economy.DistrictOpenCondition.Tags
{
    // Main tag of an "exists" district-open-condition row — the one that carries a required district.
    public struct DistrictExistOpenConditionTag : ITag
    {
    }
}
