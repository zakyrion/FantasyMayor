namespace Domains.Actions.BuildDistrictAction.Components
{
    // Marks a committed build-district action entity (the player confirmed «Збудувати»). Replaces
    // BuildDistrictActionTemplateTag on confirm; the build mechanics (spend, turns-left, …) act on it from here.
    public struct BuildDistrictActionTag
    {
    }
}
