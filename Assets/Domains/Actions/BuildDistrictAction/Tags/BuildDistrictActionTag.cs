namespace Domains.Actions.BuildDistrictAction.Tags
{
    // Discriminator for the build-district-action table (PK: ActionIdComponent). Present ⇒ this action row is a
    // district build.
    public struct BuildDistrictActionTag
    {
    }
}
