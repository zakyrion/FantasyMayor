namespace Modules.Addressable
{
    public enum AddressableStatus
    {
        Unknown = 0,
        Success,
        NotFound,
        AssetIsEmpty,
        Failed,
        Cancelled,
        Disposed
    }
}
