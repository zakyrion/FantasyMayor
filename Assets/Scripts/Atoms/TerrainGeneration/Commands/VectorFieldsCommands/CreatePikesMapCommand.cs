using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreatePikesMapCommand : MonoBehaviour
{
    //Згенерувати піки для гір котрі будуть спускатись вниз з первним кроком по висоті а далі це перемножати з мапою регіону
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
